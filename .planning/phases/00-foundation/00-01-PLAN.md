---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "CLAUDE.md contains the project identity, repo structure, and voice summary so any Claude session starts with full context"
    - "CLAUDE.md contains the safety requirements and scope boundaries that govern every tool"
    - "CLAUDE.md references the Profile Interchange Format and where to find it"
    - "CLAUDE.md lists the repo folder structure with purpose of each directory"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project instructions for all Claude sessions"
      contains: "El Sistema Eres Tu"
  key_links:
    - from: "CLAUDE.md"
      to: "shared/profile-format.md"
      via: "explicit file reference"
      pattern: "shared/profile-format"
    - from: "CLAUDE.md"
      to: "shared/safety-protocol.md"
      via: "explicit file reference"
      pattern: "shared/safety-protocol"
---

<objective>
Write the real project CLAUDE.md — the instructions file every Claude session reads to understand what N1 is, how it's structured, and what rules govern all work on it.

Purpose: The current CLAUDE.md is a GSD auto-generated stub with empty sections. This task fills every section with actual project content so no Claude session starts blind.

Output: A complete CLAUDE.md that any Claude executor can read and immediately understand: project identity, directory structure, conventions, safety requirements, and key file references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@plans/MASTER-PLAN-v2.md
@plans/REVIEW-SYNTHESIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current stub — read first to understand GSD section markers that must be preserved)
    - plans/MASTER-PLAN-v2.md (project identity, constraints, profile format reference, career taxonomy)
    - plans/REVIEW-SYNTHESIS.md (constraints section: scope boundaries, safety requirements)
    - .planning/ROADMAP.md (phase list, tool directory for reference)
  </read_first>
  <action>
Overwrite CLAUDE.md with a complete project instructions file. The GSD section markers (GSD:project-start/end, GSD:stack-start/end, etc.) MUST be preserved exactly — only fill in the content between them.

Sections to fill:

**Project section** (between GSD:project-start and GSD:project-end):
Already has content — enhance with:

- Full constraints list (all 7 from MASTER-PLAN-v2.md CONSTRAINTS section)
- Link to profile format: `shared/profile-format.md`
- Link to shared foundation: `shared/` directory

**Stack section** (between GSD:stack-start and GSD:stack-end):
Replace "not yet documented" with:

```
- Language: Markdown (SKILL.md files), YAML (profiles, frontmatter), HTML/CSS/JS (static web tools)
- AI platform: Claude Skills (SKILL.md format), ChatGPT GPTs (instructions.md + knowledge files)
- Hosting: GitHub Pages (zero cost, static only)
- No build pipeline — pure static files, no npm, no bundler
- No backend, no server, no database — localStorage only for web tools
```

**Conventions section** (between GSD:conventions-start and GSD:conventions-end):
Replace "not yet established" with:

```
## Conventions

### Language
- Code, filenames, YAML keys: English
- User-facing content (SKILL.md prompts, web copy): Spanish (Colombian voseo)
- Comments and documentation: English

### File organization
- `shared/` — files loaded by all skills and tools (DRY)
- `skills/` — one subdirectory per Claude Skill
- `gpts/` — one subdirectory per ChatGPT GPT
- `web/` — static HTML/CSS/JS tools
- `plans/` — master plan and architecture docs (not committed to skills)
- `research/` — research base and references
- `.planning/` — GSD planning artifacts

### Profile Interchange Format
Every tool that produces assessment data must output YAML compatible with `shared/profile-format.md`.
Profile format version: v1. Schema defined at `shared/profile-format.md`.
Blank template at `shared/profile-template.yaml`.

### Skill architecture (DRY rule)
Every SKILL.md MUST reference shared/ files rather than duplicating content:
- `shared/prompting-stack.md` — all prompting techniques
- `shared/voice-guide.md` — voice and tone rules
- `shared/safety-protocol.md` — MIND-SAFE, scope disclaimer, crisis resources
- `shared/research-base.md` — condensed citations (~4K tokens)
Never copy content from shared/ into a skill. Reference it.

### Safety (non-negotiable)
1. Scope disclaimer required: every tool, every session start
2. MIND-SAFE crisis detection: defined in `shared/safety-protocol.md`
3. Never use therapeutic labels with users (say "career guidance" not "therapy")
4. Assessment pacing: 4-6 week minimum between retakes of same instrument
5. Cultural calibration: high MLQ-Search is normative in Colombia, CAAS Control runs lower
6. Refer to human professional when: active crisis, clinical symptoms, legal/financial decisions

### Commit format
Conventional commits. Types: feat | fix | chore | docs | refactor | test
Example: `feat: n1-busca-adentro skill — values dimension`

### No therapeutic modality labels in user-facing text
WRONG: "This uses ACT defusion techniques"
RIGHT: "Let's look at what matters most to you and what might be getting in the way"
```

**Architecture section** (between GSD:architecture-start and GSD:architecture-end):
Replace "not yet mapped" with:

```
## Architecture

### System layers
1. Meta-theory: Protean Career Theory + Career Chaos Theory + Planned Happenstance + SDT
2. Integrative spine: SCCT (Lent, Brown & Hackett) + Schlossberg 4S
3. Self-knowledge: Busca Adentro — 4 dimensions (Values → Strengths → Identity → Purpose)
4. Career action: Construye Afuera — tools adapted per career path (Stay & Grow | Move | Go Independent | Pause & Reset)

### Entry points
- Claude Skills: skills/[name]/SKILL.md — loaded by user into Claude Projects
- ChatGPT GPTs: gpts/[name]/instructions.md + knowledge/ files
- Static web: web/[tool]/index.html — pure HTML/JS, hosted on GitHub Pages

### Data flow
User session → triage (crisis/exploration/growth + career path) → recommended tool sequence
→ YAML profile produced → profile portable across tools (clipboard copy/paste)
→ Construye Afuera reads profile → career action plan adapted per path

### Shared foundation (loaded by all skills)
shared/prompting-stack.md → shared/voice-guide.md → shared/safety-protocol.md → shared/research-base.md
```

Keep the GSD:workflow-start/end section exactly as-is. Keep GSD:profile-start/end section exactly as-is.
</action>
<verify>
<automated>grep -l "shared/profile-format.md" /c/N1/CLAUDE.md && grep -l "shared/safety-protocol.md" /c/N1/CLAUDE.md && grep -l "MIND-SAFE" /c/N1/CLAUDE.md && grep -l "GSD:project-start" /c/N1/CLAUDE.md</automated>
</verify>
<done> - CLAUDE.md contains all GSD section markers (unchanged) - CLAUDE.md references shared/profile-format.md explicitly - CLAUDE.md references shared/safety-protocol.md explicitly - CLAUDE.md contains "MIND-SAFE" in safety conventions - CLAUDE.md describes the 4-directory structure (shared/, skills/, gpts/, web/) - CLAUDE.md contains the scope disclaimer requirement - CLAUDE.md contains the DRY rule for shared/ files
</done>
</task>

</tasks>

<verification>
- `grep "shared/profile-format.md" CLAUDE.md` returns a match
- `grep "MIND-SAFE" CLAUDE.md` returns a match
- `grep "GSD:project-start" CLAUDE.md` returns a match (markers preserved)
- `grep "scope disclaimer" CLAUDE.md` returns a match
- `grep "shared/" CLAUDE.md | wc -l` returns 4+ lines (multiple shared/ references)
</verification>

<success_criteria>
CLAUDE.md is complete and any Claude session that reads it will know: what N1 is, where files live, what rules govern all work, what safety requirements are non-negotiable, and where to find the profile format and shared foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md`
</output>

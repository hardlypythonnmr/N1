---
phase: 00-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/profile-format.md
  - shared/profile-template.yaml
autonomous: true
requirements:
  - FOUN-02
  - FOUN-03

must_haves:
  truths:
    - "shared/profile-format.md documents the complete YAML schema so any tool author knows exactly what fields to produce"
    - "shared/profile-template.yaml is a blank YAML file users can copy-paste and fill in"
    - "The schema includes version field and version protocol so future changes are backward-compatible"
    - "Every major section (triage, values, strengths, identity, purpose, enrichment, synthesis, evolution) is documented with field names, types, and allowed values"
  artifacts:
    - path: "shared/profile-format.md"
      provides: "Canonical schema documentation for the Profile Interchange Format v1"
      contains: "version protocol"
    - path: "shared/profile-template.yaml"
      provides: "Blank YAML template users can copy"
      contains: "identity_status: null"
  key_links:
    - from: "shared/profile-format.md"
      to: "shared/profile-template.yaml"
      via: "explicit link in format doc"
      pattern: "profile-template.yaml"
    - from: "shared/profile-format.md"
      to: "CLAUDE.md"
      via: "referenced as canonical schema source"
      pattern: "profile-format"
---

<objective>
Create the Profile Interchange Format v1 — the canonical YAML schema that every N1 tool produces and consumes. This is load-bearing infrastructure: all static web tools export this format, all Claude Skills import it, all GPTs produce it.

Purpose: Without a defined schema, every tool will produce incompatible YAML and the "paste your profile to get richer results" feature cannot work.

Output:

- `shared/profile-format.md` — complete schema documentation with field definitions, allowed values, version protocol, and example populated profile
- `shared/profile-template.yaml` — blank template users copy to start their profile
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@plans/MASTER-PLAN-v2.md
</context>

<interfaces>
<!-- Complete Profile Interchange Format v1 YAML schema — extracted from plans/MASTER-PLAN-v2.md lines 884-1003 -->
<!-- This is the EXACT schema to implement — do not deviate from these field names -->

```yaml
# ════════════════════════════════════════
# N1 Self-Knowledge Profile v1
# El Sistema Eres Tu — Busca Adentro
# ════════════════════════════════════════

version: "1.0"
created: null # ISO 8601 timestamp
updated: null # ISO 8601 timestamp
language: "es" # es | en

# ── TRIAGE ──────────────────────────────
triage:
  situation: null # crisis | exploration | growth
  career_path: null # stay_grow | move | go_independent | pause_reset
  specific_move: null # from career path taxonomy
  urgency: null # immediate | 3_months | 6_months | exploratory

# ── VALUES (ACT + Schwartz) ────────────
values:
  completed: false
  mode: null # quick | deep
  core_values: [] # ranked list of top 5
  schwartz_profile: # 10 basic values scored
    self_direction: null
    stimulation: null
    hedonism: null
    achievement: null
    power: null
    security: null
    conformity: null
    tradition: null
    benevolence: null
    universalism: null
  value_conflicts: []
  bulls_eye:
    work: null # 1-10 alignment score
    relationships: null
    personal_growth: null
    leisure: null
  career_implications: []

# ── STRENGTHS (VIA + Bandura + Flow + Big Five) ──
strengths:
  completed: false
  mode: null
  signature_strengths: [] # top 5 from VIA
  big_five:
    openness: null
    conscientiousness: null
    extraversion: null
    agreeableness: null
    neuroticism: null
  self_efficacy:
    mastery_experiences: []
    vicarious_models: []
    social_persuasion: []
    physiological: null
  flow_activities: [] # {activity, challenge, skill, state}
  career_implications: []

# ── IDENTITY (Ibarra + Marcia + Savickas) ──
identity:
  completed: false
  mode: null
  identity_status: null # achieved | moratorium | foreclosed | diffused
  adaptability: # CAAS-12 scores
    concern: null # 1-5
    control: null
    curiosity: null
    confidence: null
  possible_selves: [] # list of explored identities
  active_experiments: [] # Ibarra-style experiments
  narrative_theme: null
  limiting_beliefs: []

# ── PURPOSE (Frankl + MLQ + Ryff) ──────
purpose:
  completed: false
  mode: null
  mlq:
    presence: null # 1-7
    search: null # 1-7
    quadrant: null # fulfilled | growing | seeking | disengaged
  meaning_sources:
    creative: null
    experiential: null
    attitudinal: null
  life_theme: null
  purpose_practice: []
  career_implications: []

# ── ENRICHMENT LAYERS ──────────────────
enrichment:
  psychological_flexibility: null # AAQ-II score
  career_decision_self_efficacy: null # CDMSE-SF score
  psycap_hero:
    hope: null
    efficacy: null
    resilience: null
    optimism: null
  emotional_patterns: null # free text from conversation
  relational_context: null # family/cultural constraints
  somatic_signals: null # body-based data from conversation

# ── CROSS-DIMENSION SYNTHESIS ──────────
synthesis:
  reinforcements: [] # where dimensions align
  conflicts: [] # where dimensions conflict
  career_recommendations: []
  next_actions: []

# ── EVOLUTION ──────────────────────────
evolution:
  assessments: [] # {date, instrument, scores} — longitudinal
  milestones: [] # {date, event, impact}
  next_reassessment: null # ISO 8601 — minimum 4-6 weeks from last
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write shared/profile-format.md</name>
  <files>shared/profile-format.md</files>
  <read_first>
    - plans/MASTER-PLAN-v2.md (lines 884-1003: the complete YAML schema — this is the canonical source)
    - plans/REVIEW-SYNTHESIS.md (constraints and design decisions that inform the format)
    - plans/FRAMEWORK-ARCHITECTURE.md (framework connections that explain why each field exists)
  </read_first>
  <action>
Create `shared/profile-format.md` with these sections in order:

**1. Header**

```markdown
# N1 Profile Interchange Format v1

> Canonical YAML schema for the N1 Self-Knowledge Profile.
> Every tool that produces assessment data MUST output YAML compatible with this schema.
> Every tool that consumes assessment data reads this schema.
> Version: 1.0 | Defined: 2026-03-23
```

**2. Version Protocol**

```markdown
## Version Protocol

- Current version: `1.0`
- Field: `version: "1.0"` — required in every profile YAML
- Backward compatibility: tools MUST accept profiles where optional fields are null or missing
- Breaking changes require major version bump (1.0 → 2.0)
- Additive changes require minor version bump (1.0 → 1.1)
- Versioned profiles: tools check `version` field and warn if incompatible
```

**3. How to Use**

```markdown
## How to Use

### Producing a profile (tools → profile)

1. Start from `shared/profile-template.yaml`
2. Fill in completed sections only — leave others as null
3. Set `completed: true` for each dimension you've finished
4. Set `updated` to current ISO 8601 timestamp

### Consuming a profile (skills/GPTs reading a profile)

1. User pastes their YAML profile into the conversation
2. Tool reads completed sections (where `completed: true`)
3. Tool uses completed data to skip covered ground and enrich responses
4. Tool treats null fields as "not yet explored"

### Portability rule

Profile YAML is portable by clipboard. No file sync, no accounts, no API.
User copies YAML from one tool, pastes into another.
```

**4. Complete Schema** — paste the full YAML schema exactly as defined in the `<interfaces>` block above. Add inline comments explaining each field's allowed values. Include all sections: version fields, triage, values, strengths, identity, purpose, enrichment, synthesis, evolution.

**5. Field Reference** — a table with columns: Field | Type | Allowed Values | Required | Source Framework. Include rows for every leaf field in the schema. Key rows to include:

- `version` | string | "1.0" | yes | —
- `language` | string | "es" \| "en" | yes | —
- `triage.situation` | enum | crisis \| exploration \| growth | yes (on completion) | Schlossberg 4S
- `triage.career_path` | enum | stay_grow \| move \| go_independent \| pause_reset | yes (on completion) | N1 taxonomy
- `values.completed` | boolean | true \| false | yes | —
- `identity.identity_status` | enum | achieved \| moratorium \| foreclosed \| diffused | no | Marcia (1966)
- `identity.adaptability.*` | float | 1-5 | no | CAAS (Savickas)
- `purpose.mlq.presence` | float | 1-7 | no | MLQ (Steger 2006)
- `purpose.mlq.quadrant` | enum | fulfilled \| growing \| seeking \| disengaged | no | N1 derived
- `enrichment.psychological_flexibility` | float | 7-49 | no | AAQ-II (Hayes)

**6. Example Populated Profile** — show a realistic partial profile with triage + values completed, other sections null. Use Colombian context: senior software engineer at outsourcing firm, considering going independent.

**7. Career Path Taxonomy Reference** — list all 4 macro-categories with their sub-moves (from MASTER-PLAN-v2.md lines 530-566). This gives context for `triage.specific_move` field values.
</action>
<verify>
<automated>grep -c "version protocol" /c/N1/shared/profile-format.md && grep -c "career_path" /c/N1/shared/profile-format.md && grep -c "identity_status" /c/N1/shared/profile-format.md && grep -c "mlq" /c/N1/shared/profile-format.md</automated>
</verify>
<done> - shared/profile-format.md exists - Contains "version protocol" section - Contains complete YAML schema (all 9 sections: version, triage, values, strengths, identity, purpose, enrichment, synthesis, evolution) - Contains field reference table - Contains example populated profile - Contains career path taxonomy reference
</done>
</task>

<task type="auto">
  <name>Task 2: Write shared/profile-template.yaml</name>
  <files>shared/profile-template.yaml</files>
  <read_first>
    - shared/profile-format.md (just created in Task 1 — use the schema defined there as source of truth)
  </read_first>
  <action>
Create `shared/profile-template.yaml` — a blank YAML template users copy to start their profile.

The file must:

1. Start with a comment block: `# N1 Self-Knowledge Profile v1 — blank template` and instructions for use
2. Include ALL fields from the schema with null/false/[] values
3. Include inline comments on fields with constrained values (enums, ranges)
4. Be syntactically valid YAML (test: every key has a value, no bare keys)

The exact content is the full YAML schema from the `<interfaces>` block above, with:

- All string fields set to `null`
- All boolean fields set to `false`
- All list fields set to `[]`
- All numeric fields set to `null`
- All comments from the schema preserved
- `version: "1.0"` set (not null — this is a fixed value)
- `language: "es"` set as default (not null — default is Spanish)
- `created: null` and `updated: null`

Add a comment at the top:

```yaml
# ════════════════════════════════════════
# N1 Self-Knowledge Profile v1
# Blank template — copy this file and fill it in as you complete each dimension
#
# How to use:
# 1. Copy this file
# 2. Complete an N1 assessment (Claude Skill, GPT, or static web tool)
# 3. Paste the YAML output from that tool into the matching section
# 4. Set completed: true for that section
# 5. Paste into any other N1 tool to get profile-enhanced results
# ════════════════════════════════════════
```

  </action>
  <verify>
    <automated>grep "identity_status: null" /c/N1/shared/profile-template.yaml && grep 'version: "1.0"' /c/N1/shared/profile-template.yaml && grep "completed: false" /c/N1/shared/profile-template.yaml</automated>
  </verify>
  <done>
    - shared/profile-template.yaml exists
    - Contains `version: "1.0"` (not null)
    - Contains `identity_status: null`
    - Contains `completed: false` (for each dimension)
    - File is syntactically valid YAML (no parse errors)
    - Contains usage instructions in comment block
  </done>
</task>

</tasks>

<verification>
- `ls shared/profile-format.md shared/profile-template.yaml` — both files exist
- `grep "version protocol" shared/profile-format.md` — version protocol section present
- `grep "identity_status: null" shared/profile-template.yaml` — blank template correct
- `grep 'version: "1.0"' shared/profile-template.yaml` — version field set
- `wc -l shared/profile-format.md` — should be 150+ lines (substantial documentation)
</verification>

<success_criteria>
Any tool author can read shared/profile-format.md and know exactly what YAML to produce. Any user can copy shared/profile-template.yaml and fill it in incrementally as they complete assessments.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-02-SUMMARY.md`
</output>

---
phase: quick
plan: 260324-kd3
type: execute
wave: 1
depends_on: []
files_modified:
  - web/styles.css
  - web/index.html
  - web/compass/index.html
  - web/ghost-check/index.html
  - web/mlq/index.html
  - web/flow-logger/index.html
  - web/runway/index.html
  - web/bulls-eye/index.html
  - web/profile/index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 8 pages use ~1100px max-width for content on desktop, not 640px"
    - "Compass shows only one screen at a time (no stacking bug)"
    - "All 13 visual QA issues from VISUAL-QA-REPORT.md are resolved"
    - "Pages remain usable on mobile (breakpoints preserve readability)"
  artifacts:
    - path: "web/styles.css"
      provides: "Updated .container max-width to 1100px with mobile breakpoint"
    - path: "web/compass/index.html"
      provides: "Screen elements without flex in class attribute"
  key_links:
    - from: "web/styles.css"
      to: "all tool pages"
      via: ".container class and shared design tokens"
      pattern: "max-width.*1100"
---

<objective>
Fix all 13 visual issues from VISUAL-QA-REPORT.md and convert all 8 pages from mobile-first narrow columns (480-640px) to desktop-first responsive layout (~1100px max-width centered, with mobile breakpoints).

Purpose: Desktop users currently see a mobile-width layout with 280-400px of dead space per side. This makes the app look unfinished on desktop.
Output: All pages render at proper desktop width with all visual defects resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@screenshots/VISUAL-QA-REPORT.md
@web/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared CSS + Compass Critical Fix + Responsive Overhaul of All Pages</name>
  <files>web/styles.css, web/compass/index.html, web/index.html, web/ghost-check/index.html, web/mlq/index.html, web/flow-logger/index.html, web/runway/index.html, web/bulls-eye/index.html, web/profile/index.html</files>
  <action>
**A. Shared CSS (web/styles.css):**
1. Change `.container` max-width from `640px` to `1100px`.
2. In the existing `@media (max-width: 640px)` breakpoint, add `.container { max-width: 100%; }` (already there, just confirm).
3. Add a new utility class `.container-narrow` with `max-width: 640px; margin: 0 auto;` for assessment question flows that should stay narrow (Compass questions, MLQ questions) — these are single-focus screens that read better narrow.

**B. Responsive width overhaul — all Tailwind pages:**
Every page uses `max-w-[640px]` on `<main>`, nav bar, and bottom nav. Change ALL of these to `max-w-[1100px]`. Specifically:

- `web/index.html`: Change all 8 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/ghost-check/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/flow-logger/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/bulls-eye/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/runway/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/mlq/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`
- `web/profile/index.html`: Change all 3 instances of `max-w-[640px]` to `max-w-[1100px]`

NOTE: Compass uses custom `<style>` block, not the shared .container class for its main elements. Its screens already use Tailwind classes with `flex flex-col items-center` — the fix is in part C below.

**C. CRITICAL — Compass screen overlap (Issue #1):**
In `web/compass/index.html`, the `<main>` elements at lines ~214, ~328, ~386 have `flex` in their class attribute. The CSS rule `.screen { display: none; }` / `.screen.active { display: flex; }` means the inline `flex` class from Tailwind overrides the `display: none` — so all screens show at once.

- Line ~214: `<main id="screen-start" class="screen active relative z-10 pt-24 pb-32 px-4 flex flex-col items-center">` — Remove `flex` and `flex-col` and `items-center` from the class. Instead, add these properties to the `.screen.active` CSS rule in the `<style>` block: `.screen.active { display: flex; flex-direction: column; align-items: center; }`
- Line ~328: `<main id="screen-questions" class="screen relative z-10 pt-24 pb-32 px-4 flex flex-col items-center">` — Remove `flex`, `flex-col`, `items-center`
- Line ~386: `<main id="screen-results" class="screen relative z-10 pt-24 pb-32 px-4 max-w-[640px] mx-auto overflow-hidden">` — Remove nothing here (no `flex`), but change `max-w-[640px]` to `max-w-[1100px]`
- Update the `<style>` block: `.screen.active { display: flex; flex-direction: column; align-items: center; }`
  </action>
  <verify>
  <automated>grep -c "max-w-\[640px\]" web/index.html web/ghost-check/index.html web/flow-logger/index.html web/bulls-eye/index.html web/runway/index.html web/mlq/index.html web/profile/index.html web/compass/index.html 2>/dev/null | grep -v ":0$" | wc -l</automated>
  Should return 0 (no files with remaining 640px max-width). Also verify: `grep "max-width: 1100px" web/styles.css` returns the .container rule. Also verify: `grep "class=\"screen" web/compass/index.html` shows NO `flex` in screen-start or screen-questions class attributes.
  </verify>
  <done>All pages use 1100px max-width. Compass screen overlap bug fixed. Shared .container updated.</done>
  </task>

<task type="auto">
  <name>Task 2: Fix All 13 Visual QA Issues (Per-Page Fixes)</name>
  <files>web/profile/index.html, web/flow-logger/index.html, web/ghost-check/index.html, web/mlq/index.html, web/runway/index.html, web/bulls-eye/index.html, web/index.html</files>
  <action>
Fix each remaining issue from VISUAL-QA-REPORT.md priority list. Task 1 already handles Issue #1 (Compass) and the width overhaul. This task handles Issues #2-13:

**Issue #2 — Profile heading "N1" orphan (HIGH):**
In `web/profile/index.html` line ~302, change:
`<h2 class="font-headline text-2xl font-bold text-tertiary tracking-tight">Panel de Perfil Integrado<br/>N1</h2>`
to:
`<h2 class="font-headline text-2xl font-bold text-tertiary tracking-tight">Panel de Perfil Integrado N1</h2>`
Remove the `<br/>` — with wider container it fits on one line. Add `white-space: nowrap` as a safety: change to `<h2 class="font-headline text-2xl font-bold text-tertiary tracking-tight whitespace-nowrap">`.

**Issue #3 — Flow Logger scatter chart low contrast (HIGH):**
In `web/flow-logger/index.html`, find the Chart.js scatter chart configuration in the `<script>` section. Make these changes:

- Change dot/point color from the current orange on dark to a brighter, more visible color: use `backgroundColor: 'rgba(255, 160, 120, 0.9)'` and `borderColor: '#fff'` with `borderWidth: 1` for each point to create contrast
- Increase `pointRadius` to at least 6 (from whatever it currently is)
- For axis tick labels and grid lines: set `color: 'rgba(229, 227, 255, 0.5)'` (lighter text) on both x and y axis ticks, and `color: 'rgba(70, 70, 92, 0.6)'` on grid
- For zone labels (the custom plugin that draws Flujo, Ansiedad, etc.): increase font size and use `rgba(229, 227, 255, 0.6)` for better visibility against the dark background

**Issue #4 — Ghost Check LinkedIn preview dark mode (MEDIUM):**
In `web/ghost-check/index.html`, find the LinkedIn preview card section (the scorecard preview or share preview area). Add dark-mode styling:

- Wrap the preview in a container with `bg-surface-container-high border border-outline-variant/30 rounded-lg p-3`
- If the preview canvas/image has a white background, that is fine (it is a social share image) — just ensure the surrounding wrapper has a dark border and dark background so it does not float as a white box in the dark layout

**Issue #5 — MLQ-10 intro empty space (HIGH):**
In `web/mlq/index.html`, find the intro/start screen (before the assessment begins). Below the CTA button, add a visual teaser block:

- Add a `<div>` with class `mt-8 p-5 rounded-xl bg-surface-container/60 border border-outline-variant/20`
- Inside, add a heading: `<h3 class="font-headline text-sm font-bold text-tertiary uppercase tracking-wider mb-3">Que vas a descubrir</h3>`
- Add a simple 2x2 grid showing the 4 quadrant labels: `<div class="grid grid-cols-2 gap-3">` with 4 items, each a small card with an icon and label for the MLQ quadrants: "Alta Presencia + Alta Busqueda" (Comprometido), "Alta Presencia + Baja Busqueda" (Satisfecho), "Baja Presencia + Alta Busqueda" (Buscando), "Baja Presencia + Baja Busqueda" (Pasivo). Use `text-xs` for labels, `text-on-surface-variant` for color, with a small colored dot or icon for each.

**Issue #6 — Ghost Check input column width (HIGH):**
Already mostly handled by Task 1 (max-w change to 1100px). But the textarea should specifically be wider. In `web/ghost-check/index.html`, find the textarea element and ensure it has `w-full` class. If the textarea or its parent card has a max-width constraint smaller than the main container, remove it or increase to `max-w-2xl` (672px).

**Issue #7 — Profile Key Insights 3-col grid (MEDIUM):**
In `web/profile/index.html` line ~352, change:
`<div class="grid grid-cols-2 gap-3" id="key-insights-grid">`
to:
`<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3" id="key-insights-grid">`
This ensures 3 columns at desktop width (where 3 insight cards render as 3-col row, not 2+1).

**Issue #8 — Runway info card font size (MEDIUM):**
In `web/runway/index.html`, find the three top info cards (Ahorro, Plazo, Contexto). Their body text is too small (~11px). Find the text elements inside these cards and change from `text-xs` to `text-sm` (14px). If they use a custom font-size, change to `font-size: 0.875rem`.

**Issue #9 — Ghost Check signal columns equalize heights (MEDIUM):**
In `web/ghost-check/index.html`, find the 3-column signal layout (Senales Rojas / Amarillas / Verdes). These columns should stretch to equal height. Find the parent container — likely a flex or grid wrapper — and ensure it uses `items-stretch` (if flex) or `grid-rows-[1fr]` (if grid). Each column card should have `h-full` or `min-h-full` to fill the available space.

**Issue #10 — Bull's Eye visual separator + alignment output prominence (MEDIUM + LOW, Issues #11 and #13):**
In `web/bulls-eye/index.html`:

- Between the canvas diagram section and the slider section, add a horizontal rule or visual separator: `<hr class="border-outline-variant/30 my-6 lg:hidden">` (only visible when stacked on smaller screens). On desktop side-by-side layout, add a vertical divider via the parent flex container: add `lg:divide-x lg:divide-outline-variant/30` to the parent flex container.
- Find the "TU ALINEACION ACTUAL" percentage readout (id `live-alignment-msg` or similar). Make it more prominent: wrap in a card-like container with `bg-surface-container-high/50 border border-outline-variant/30 rounded-xl p-4 text-center`. Increase the percentage font size to `text-3xl font-bold text-primary`.

**Issue #12 — Landing tool grid 3-col at wide viewports (LOW):**
In `web/index.html` line ~423, change:
`<div class="grid grid-cols-1 sm:grid-cols-2 gap-4" role="list" aria-label="Lista de herramientas">`
to:
`<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4" role="list" aria-label="Lista de herramientas">`
This adds a 3-column layout at large viewports (1024px+), which now makes sense since the container is 1100px wide.
</action>
<verify>
<automated>grep -c "grid-cols-3" web/index.html web/profile/index.html && grep -c "whitespace-nowrap" web/profile/index.html && grep -c "items-stretch\|h-full" web/ghost-check/index.html</automated>
Should show: index.html has at least 1 grid-cols-3, profile has at least 1 grid-cols-3, profile has whitespace-nowrap, ghost-check has items-stretch or h-full for signal columns.
</verify>
<done>All 13 issues from VISUAL-QA-REPORT.md addressed: profile heading fixed, flow logger chart contrast improved, ghost check LinkedIn preview dark-wrapped, MLQ intro has visual content, profile key insights uses 3-col, ghost check input wider, runway card text bigger, signal columns equalized, bull's eye separator and alignment prominence added, landing grid uses 3-col at wide viewports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Desktop-first responsive layout across all 8 pages + all 13 visual QA fixes</what-built>
  <how-to-verify>
    1. Open each page in a browser at 1280px+ viewport width. Content should fill ~1100px centered, not a narrow 640px column.
    2. Compass page: Only one screen should be visible at a time (intro OR questions, never both).
    3. Profile: "Panel de Perfil Integrado N1" on one line, Key Insights in 3-col grid.
    4. Ghost Check: Input textarea wider, signal columns equal height, LinkedIn preview has dark wrapper.
    5. Flow Logger: Scatter chart dots and labels clearly visible against dark background.
    6. MLQ-10: Intro screen has quadrant preview teaser, not empty space.
    7. Runway: Info card text readable (14px, not 11px).
    8. Bull's Eye: Visual separator between diagram and sliders, alignment percentage prominent.
    9. Landing: Tool grid shows 3 columns at wide viewport.
    10. Resize browser to mobile width (~375px) — pages should still be usable, single column.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All `max-w-[640px]` replaced with `max-w-[1100px]` across all HTML files
- `.container` in styles.css uses `max-width: 1100px`
- Compass `<main class="screen ...">` elements have no `flex` in class attribute
- No regressions on mobile (breakpoints still work)
</verification>

<success_criteria>

- Zero instances of `max-w-[640px]` remaining in any web/ HTML file
- All 13 VISUAL-QA-REPORT.md issues resolved
- Pages render at ~1100px content width on desktop
- Compass shows only one screen at a time
- Human verification passes
  </success_criteria>

<output>
After completion, update `.planning/STATE.md` with quick task entry.
</output>
